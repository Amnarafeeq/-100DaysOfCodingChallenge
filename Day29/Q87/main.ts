// Extracting a Substring: Demonstrate how to extract the first 10 characters from a string.

function extractingASubstring(str:string):string{
    return str.substring(0,10)
}
console.log(extractingASubstring("Hello I am trying to be a web developer"));
console.log(extractingASubstring("Javascript is an interesting language."));

