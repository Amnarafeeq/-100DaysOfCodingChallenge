// Converting to Upper and Lower Case: Create a function that takes a string, converts it to uppercase, 
// then to lowercase, and logs both.

function upperAndLowerCase(): void {
    let statement:string = "Hi! I am learning Typescript."
    console.log(statement.toUpperCase());
    console.log(statement.toLowerCase());
    
}
upperAndLowerCase()