// Explain how to export a function from one
//  JavaScript file and import it into another file.

export let addNumbers = (num1:number, num2:number) => {
    return num1 / num2
}

export let subtractNumbers = (num1:number, num2:number) => {
    return num1 - num2 
}